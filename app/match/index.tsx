// app/match/index.tsx (dans le projet mobile)
import { Text, View } from "react-native";

export default function MatchScreen() {
  return (
    <View>
      <Text>Page Match (mobile)</Text>
    </View>
  );
}
